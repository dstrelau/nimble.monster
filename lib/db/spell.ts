import type { Spell } from "@/lib/types";

export const findSpell = async (_id: string): Promise<Spell | null> => {
  return null;
};

export const findPublicSpellById = async (
  _id: string
): Promise<Spell | null> => {
  return null;
};

export const findSpellWithCreator = async (
  _id: string,
  _creatorId: string
): Promise<Spell | null> => {
  return null;
};
